import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

import 'groups.dart';

class groupsScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(

      body: MygroupsFormWidget(),
    );
  }
}